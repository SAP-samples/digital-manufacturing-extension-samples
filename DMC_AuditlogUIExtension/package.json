{
    "name": "mta-project",
    "version": "0.0.1",
    "description": "Build and deployment scripts",
    "scripts": {
        "build": "mta_archives && mbt build --mtar archive",
        "deploy": "cf deploy mta_archives/archive.mtar",
        "undeploy": "cf undeploy dme-advauditapp-ui --delete-services --delete-service-keys"
    },
    "devDependencies": {
        "mbt": "^1.0.15",
        "rimraf": "3.0.0"
    }
}